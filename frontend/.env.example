# StellarTrust frontend — environment template.
# Public (NEXT_PUBLIC_*) values only; never put secrets in the frontend.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080
# Production (Vercel) — set this in the Vercel dashboard, then redeploy so the
# build picks it up (NEXT_PUBLIC_* is baked in at build time):
# NEXT_PUBLIC_API_BASE_URL=https://stellartrust.onrender.com
